```
/**
 * @param {number[]} nums
 * @return {boolean}
 */
var isArraySpecial = function(nums) {
    const len = nums.length;
    for (let i = 1; i < len; ++i){
        if (nums[i-1] % 2 == nums[i] % 2){
            return false;
        }
    }
    return true
};
```
